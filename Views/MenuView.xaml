<UserControl x:Class="AzureFable.Views.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="420">

            <TextBlock Text="Azure Fable"
                       Style="{StaticResource ScreenTitleStyle}"
                       FontSize="54"
                       Margin="0,0,0,10"/>

            

            <Button Content="Нова гра"
                    Command="{Binding StartGameCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="0,0,0,18"/>

            <Button Content="Налаштування"
                    Command="{Binding SettingsCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="0,0,0,18"/>

            <Button Content="Довідка"
                    Command="{Binding HelpCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="0,0,0,18"/>

            <Button Content="Вихід"
                    Command="{Binding ExitCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"/>

        </StackPanel>
    </Grid>
</UserControl>
